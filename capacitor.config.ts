import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'petalk-prj',
  webDir: 'dist/petalk_prj',
  bundledWebRuntime: false
};

export default config;
